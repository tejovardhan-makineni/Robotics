# Data Folder

Use this folder for generated toy datasets, robot logs, and experiment outputs.

Suggested structure:

```text
data/
├── generated/
├── logs/
├── raw/
├── processed/
└── reports/
```

Do not mix raw logs with processed training data. Keep a short dataset card beside every dataset you create.
